logging:
  level: DEBUG

observers:
  logger:
    enabled: true
    log_level: "DEBUG"

docker:
  build_docker_image: false  # Use existing images

tests:
  - name: "Test Localhost Fix"
    description: "Test that localhost environment works after template fix"
    network_environment:
      type: localhost_single_container
    services:
      server:
        name: server
        implementation:
          name: picoquic
          type: iut
        protocol:
          name: quic
          version: rfc9000
          role: server
        timeout: 30
        ports:
          - "4443:4443"
      client:
        name: client
        implementation:
          name: picoquic
          type: iut
        protocol:
          name: quic
          version: rfc9000
          role: client
          target: server
        timeout: 30
    steps:
      wait: 15