#lang ivy1.7

# Stage 4: Escalate Privileges

# Threat actors then move laterally along the target network looking for privileged credentials that will grant them even deeper access.

action hijack_credentials(src:agent, dst:agent, data:stream_data, pos:stream_pos, close:bool)

action inject_malicious_code(src:agent, dst:agent, data:stream_data, pos:stream_pos, close:bool)