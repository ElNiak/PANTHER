#lang ivy1.7

include order
include quic_infer
include file
include quic_shim
# include quic_locale
include random_value


# create a client and server. the client has an instance of TLS

# instance client : endpoint(endpoint_id.client)
# instance server : endpoint(endpoint_id.server)

instance client_server : endpoint.client_server_quic(server_addr,client_port,server_port)


