<!-- filepath: /Users/elniak/Documents/Project/PANTHER/.markdownlint.yaml -->
# Default state for all rules
default: true

# MD013/line-length - Line length
MD013:
  # Number of characters
  line_length: 120
  # Allow longer lines in code blocks
  code_blocks: true
  # Allow longer lines in tables
  tables: true

# MD033/no-inline-html - Inline HTML
MD033:
  # Allow HTML comments for source references
  allowed_elements: ["!--"]

# MD041/first-line-heading/first-line-h1 - First line in file should be a top level heading
MD041: false
