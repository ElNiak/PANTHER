#lang ivy1.7

include order
include quic_infer
include file
include quic_shim
# include quic_locale
include random_value


# create a client and server. the client has an instance of TLS

# instance client : quic_endpoint(endpoint_id.client)
# instance server : quic_endpoint(endpoint_id.server)

instance client_server : quic_endpoint.client_server_ep(server_addr,client_port,server_port)


