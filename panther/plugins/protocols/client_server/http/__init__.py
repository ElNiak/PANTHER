"""HTTP protocol plugin.

Defines protocol metadata and configuration for HTTP/1.1 and HTTP/3
testing. Manages version parameters, default ports, and test scenario
categories for HTTP conformance and interoperability testing.

See `HTTPProtocol` for the protocol manager implementation and
`HttpProtocolConfig` for configuration options.
"""
