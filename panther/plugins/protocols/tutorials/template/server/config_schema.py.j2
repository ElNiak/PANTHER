#!/usr/bin/env python3
"""
Configuration schema for {{ plugin_name }} Protocol Server plugin.
"""

from typing import Dict, Any

SCHEMA: Dict[str, Any] = {
    "server": {
        "type": "dict",
        "schema": {
            "host": {
                "type": "string",
                "default": "0.0.0.0",
                "description": "Host address to bind the server to"
            },
            "port": {
                "type": "integer",
                "default": 8080,
                "min": 1,
                "max": 65535,
                "description": "Port to listen on"
            },
            "backlog": {
                "type": "integer",
                "default": 100,
                "min": 1,
                "description": "Maximum number of pending connections"
            }
        },
        "default": {"host": "0.0.0.0", "port": 8080, "backlog": 100},
        "description": "Server binding settings"
    },
    "performance": {
        "type": "dict",
        "schema": {
            "max_clients": {
                "type": "integer",
                "default": 1000,
                "min": 1,
                "description": "Maximum number of simultaneous clients"
            },
            "threads": {
                "type": "integer",
                "default": 10,
                "min": 1,
                "description": "Number of worker threads or processes"
            },
            "timeout": {
                "type": "float",
                "default": 60.0,
                "min": 0.0,
                "description": "Idle connection timeout in seconds (0 for no timeout)"
            },
            "compression": {
                "type": "boolean",
                "default": False,
                "description": "Enable data compression"
            }
        },
        "default": {"max_clients": 1000, "threads": 10, "timeout": 60.0, "compression": False},
        "description": "Server performance settings"
    },
    "protocol_version": {
        "type": "string",
        "default": "1.0",
        "description": "Protocol version to use for compatibility"
    },
    "security": {
        "type": "dict",
        "schema": {
            "encryption": {
                "type": "boolean",
                "default": True,
                "description": "Enable connection encryption"
            },
            "cert_file": {
                "type": "string",
                "default": "",
                "description": "Server certificate file path"
            },
            "key_file": {
                "type": "string",
                "default": "",
                "description": "Server key file path"
            },
            "client_auth": {
                "type": "boolean",
                "default": False,
                "description": "Require client certificate authentication"
            }
        },
        "default": {"encryption": True, "cert_file": "", "key_file": "", "client_auth": False},
        "description": "Security settings for the server"
    }
}
