#!/usr/bin/env python3
"""
Configuration schema for {{ plugin_name }} Tester plugin.
"""

from typing import Dict, Any

SCHEMA: Dict[str, Any] = {
    "connection": {
        "type": "dict",
        "schema": {
            "host": {
                "type": "string",
                "default": "localhost",
                "description": "Hostname or IP address of the implementation under test"
            },
            "port": {
                "type": "integer",
                "default": 8080,
                "min": 1,
                "max": 65535,
                "description": "Port number of the implementation under test"
            }
        },
        "description": "Connection settings for the implementation under test"
    },
    "test_timeout": {
        "type": "integer",
        "default": 30,
        "min": 1,
        "description": "Timeout in seconds for individual tests"
    },
    "max_retries": {
        "type": "integer",
        "default": 3,
        "min": 0,
        "description": "Maximum number of retries for failed tests"
    },
    "test_level": {
        "type": "string",
        "default": "basic",
        "allowed": ["basic", "extended", "comprehensive"],
        "description": "Level of testing to perform"
    },
    "debug": {
        "type": "boolean",
        "default": False,
        "description": "Enable debug logging for tests"
    }
    # Add additional configuration parameters as needed
}
