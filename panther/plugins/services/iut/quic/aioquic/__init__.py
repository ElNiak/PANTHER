"""aioquic QUIC IUT service plugin.

Python asyncio-based QUIC implementation. Extends
`PythonQUICServiceManager` with virtualenv setup and
async event loop integration.

See `AioquicServiceManager` for the service manager and
`AioquicConfig` for configuration options.
"""
