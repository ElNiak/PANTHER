versions:
  draft_29:
  rfc9000:

environments:
  client:
    - ROLE: client
    - TARGET: picoquic_server
    - MESSAGE: ""
  server:
    - ROLE: server
    - SSLKEYLOGFILE: "/app/logs/sslkeylogfile.txt"

binary-args:
  client:
    - ticket-file
    - initial-version
    - cert-file
    - key-file
    - alpn-value
    - interface-value
    - port-value
    - destination-value
  server:
    - qlog-file
    - initial-version
    - cert-file
    - key-file
    - alpn-value
    - interface-value
    - port-value
    - source-value

# Might be useless now
templates:
  server: "server_command.j2"
  client: "client_command.j2"