#!/usr/bin/env python3
"""
Configuration schema for {{ plugin_name }} Network Environment plugin.
"""

from typing import Dict, Any

SCHEMA: Dict[str, Any] = {
    "topology": {
        "type": "string",
        "default": "star",
        "allowed": ["star", "mesh", "ring", "bus", "custom"],
        "description": "Network topology configuration"
    },
    "latency": {
        "type": "string",
        "default": "0ms",
        "description": "Base network latency (e.g., '10ms', '100ms')"
    },
    "bandwidth": {
        "type": "string",
        "default": "1Gbps",
        "description": "Network bandwidth limit (e.g., '100Mbps', '1Gbps')"
    },
    "packet_loss": {
        "type": "float",
        "default": 0.0,
        "min": 0.0,
        "max": 100.0,
        "description": "Packet loss rate in percentage"
    },
    "jitter": {
        "type": "string",
        "default": "0ms",
        "description": "Network jitter (variation in latency)"
    },
    "devices": {
        "type": "list",
        "schema": {
            "type": "dict",
            "schema": {
                "name": {
                    "type": "string",
                    "required": True,
                    "description": "Device name"
                },
                "type": {
                    "type": "string",
                    "default": "host",
                    "allowed": ["host", "router", "switch", "gateway"],
                    "description": "Device type"
                },
                "ip": {
                    "type": "string",
                    "default": "auto",
                    "description": "Device IP address (or 'auto' for automatic assignment)"
                }
            }
        },
        "default": [],
        "description": "Network devices to create in the environment"
    },
    "custom_links": {
        "type": "list",
        "schema": {
            "type": "dict",
            "schema": {
                "source": {
                    "type": "string",
                    "required": True,
                    "description": "Source device name"
                },
                "target": {
                    "type": "string",
                    "required": True,
                    "description": "Target device name"
                },
                "latency": {
                    "type": "string",
                    "default": "inherit",
                    "description": "Link-specific latency (or 'inherit' to use global value)"
                },
                "bandwidth": {
                    "type": "string",
                    "default": "inherit",
                    "description": "Link-specific bandwidth (or 'inherit' to use global value)"
                }
            }
        },
        "default": [],
        "description": "Custom network links for 'custom' topology"
    }
}
