"""Plugin configuration models.

Currently empty — ProtocolPluginConfig was removed as dead code (never instantiated,
no plugin extended it). Plugin-specific config classes are defined in each plugin's
``config_schema.py`` and extend ``BaseConfig`` or ``ServiceConfig`` directly.
"""
