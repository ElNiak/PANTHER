#!/usr/bin/env python3
"""
Configuration schema for {{ plugin_name }} IUT plugin.
"""

from typing import Dict, Any

SCHEMA: Dict[str, Any] = {
    "host": {
        "type": "string",
        "default": "localhost",
        "description": "Hostname or IP address for the {{ plugin_name }} service"
    },
    "port": {
        "type": "integer",
        "default": 8080,
        "min": 1,
        "max": 65535,
        "description": "Port number for the {{ plugin_name }} service"
    },
    "timeout": {
        "type": "integer",
        "default": 30,
        "min": 1,
        "description": "Timeout in seconds for operations"
    },
    "debug": {
        "type": "boolean",
        "default": False,
        "description": "Enable debug logging"
    },
    # Add additional configuration parameters as needed
}
