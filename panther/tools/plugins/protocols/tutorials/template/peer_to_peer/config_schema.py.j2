#!/usr/bin/env python3
"""
Configuration schema for {{ plugin_name }} Peer-to-Peer Protocol plugin.
"""

from typing import Dict, Any

SCHEMA: Dict[str, Any] = {
    "node": {
        "type": "dict",
        "schema": {
            "node_id": {
                "type": "string",
                "default": "",  # Will be auto-generated if empty
                "description": "Unique identifier for this node (empty for auto-generation)"
            },
            "host": {
                "type": "string",
                "default": "0.0.0.0",
                "description": "Host address to bind the node to"
            },
            "port": {
                "type": "integer",
                "default": 8080,
                "min": 1,
                "max": 65535,
                "description": "Port to listen on"
            }
        },
        "default": {"node_id": "", "host": "0.0.0.0", "port": 8080},
        "description": "Node binding settings"
    },
    "bootstrap_peers": {
        "type": "list",
        "schema": {
            "type": "dict",
            "schema": {
                "host": {
                    "type": "string",
                    "required": True,
                    "description": "Host address of bootstrap peer"
                },
                "port": {
                    "type": "integer",
                    "required": True,
                    "min": 1,
                    "max": 65535,
                    "description": "Port of bootstrap peer"
                }
            }
        },
        "default": [],
        "description": "List of bootstrap peers to connect to on startup"
    },
    "discovery": {
        "type": "dict",
        "schema": {
            "method": {
                "type": "string",
                "default": "bootstrap",
                "allowed": ["bootstrap", "dht", "mdns", "manual"],
                "description": "Peer discovery method"
            },
            "interval": {
                "type": "integer",
                "default": 60,
                "min": 0,
                "description": "Automatic discovery interval in seconds (0 for manual only)"
            },
            "max_peers": {
                "type": "integer",
                "default": 50,
                "min": 1,
                "description": "Maximum number of peers to maintain"
            }
        },
        "default": {"method": "bootstrap", "interval": 60, "max_peers": 50},
        "description": "Peer discovery settings"
    },
    "protocol_version": {
        "type": "string",
        "default": "1.0",
        "description": "Protocol version to use for compatibility"
    },
    "security": {
        "type": "dict",
        "schema": {
            "encryption": {
                "type": "boolean",
                "default": True,
                "description": "Enable connection encryption"
            },
            "verify_peers": {
                "type": "boolean",
                "default": True,
                "description": "Verify peer identities"
            },
            "cert_file": {
                "type": "string",
                "default": "",
                "description": "Certificate file path"
            },
            "key_file": {
                "type": "string",
                "default": "",
                "description": "Key file path"
            }
        },
        "default": {"encryption": True, "verify_peers": True, "cert_file": "", "key_file": ""},
        "description": "Security settings for peer connections"
    }
}
