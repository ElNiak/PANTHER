#!/usr/bin/env python3
"""
Configuration schema for {{ plugin_name }} Protocol Client plugin.
"""

from typing import Dict, Any

SCHEMA: Dict[str, Any] = {
    "connection": {
        "type": "dict",
        "schema": {
            "timeout": {
                "type": "float",
                "default": 5.0,
                "min": 0.1,
                "description": "Connection timeout in seconds"
            },
            "retry_attempts": {
                "type": "integer",
                "default": 3,
                "min": 0,
                "description": "Number of connection retry attempts"
            },
            "retry_delay": {
                "type": "float",
                "default": 1.0,
                "min": 0.0,
                "description": "Delay between retry attempts in seconds"
            }
        },
        "default": {"timeout": 5.0, "retry_attempts": 3, "retry_delay": 1.0},
        "description": "Connection settings"
    },
    "protocol_version": {
        "type": "string",
        "default": "1.0",
        "description": "Protocol version to use for compatibility"
    },
    "security": {
        "type": "dict",
        "schema": {
            "encryption": {
                "type": "boolean",
                "default": True,
                "description": "Enable connection encryption"
            },
            "verify_cert": {
                "type": "boolean",
                "default": True,
                "description": "Verify server certificate"
            },
            "cert_file": {
                "type": "string",
                "default": "",
                "description": "Client certificate file path (if required)"
            },
            "key_file": {
                "type": "string",
                "default": "",
                "description": "Client key file path (if required)"
            }
        },
        "default": {"encryption": True, "verify_cert": True, "cert_file": "", "key_file": ""},
        "description": "Security settings for the connection"
    },
    "performance": {
        "type": "dict",
        "schema": {
            "batch_size": {
                "type": "integer",
                "default": 1024,
                "min": 1,
                "description": "Maximum batch size for sending data"
            },
            "compression": {
                "type": "boolean",
                "default": False,
                "description": "Enable data compression"
            }
        },
        "default": {"batch_size": 1024, "compression": False},
        "description": "Performance tuning options"
    }
}
