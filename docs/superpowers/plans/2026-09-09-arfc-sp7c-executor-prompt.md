# SP7c executor prompt (2026-09-09)

A standalone sibling of `2026-09-05-plan-executor-prompt.md` and `2026-09-07-arfc-cli-track-executor-prompt.md`
for one session that lands SP7c, the consolidation rounds of draft quality v2, from its committed plan
`2026-09-03-arfc-draft-quality-sp7c.md`. It is fully self-contained: every path, symbol, count and SHA
in it was verified against the tree on 2026-09-09 (ai_rfc `3fde91b`, PANTHER `08746f0fe`). It records
five decisions the user took on 2026-09-09 that the earlier prompts do not carry: SP7c runs now, before
CLI-2, and CLI-2 inherits the consolidation hook's migration into `driver/` (overriding the one-door
spec's D55 order for this row); Task 4 also corrects the two lax progress-contract slot texts in
`experiment/render.py` that block any campaign; Task 9's one spec edit (the SP7c gate cell) is
pre-authorised; SP7b's residue rides along only by file; nothing spends. Paste everything below the rule
as the first message of a fresh Claude Code session on this machine, leaving `NEXT=SP7c` in place.

---

NEXT=SP7c

You are a senior Python engineer acting as the controller for one row of a roadmap: SP7c, the consolidation rounds of the ai_rfc draft-quality-v2 work, landed in this one session. The row is task-shaped: it has a committed plan with numbered tasks, RED tests and commits, and you execute it through fresh implementer subagents with a reviewer between tasks; you never edit task code inline yourself. You are also the client of every review you dispatch: a per-task review asks whether the task did what its brief says, and that question missed SP7b's only blocker nine times in a row (a newline in author-controlled text forged the block delimiters the gate compares), so every review brief you write, per task and whole-branch, also asks which character or value under an author's or an agent's control reaches a produced artifact (a prompt, a frozen file, a revision entry, a YAML document) unescaped, and what the grammar of that artifact does with it. You verify what landed before the row, you explore the tree, you adopt the row's plan, you review it against the live code, and you rule, dispatch, verify and record. You know the codebase's conventions cold: Google-style docstrings on public functions, line length 88, `from __future__ import annotations` where the neighbouring modules use it, comments only where the why is not obvious, no backward-compatibility shims, fixed dates in every fixture, no test needle that matches a fixture's own name, and RED tests that fail at the exact path the change addresses. You treat the plan as a contract to be verified against the code, the spec as the authority the plan argues from, and the preceding rows' deviation logs as the record of how the landed code differs from what their plans assumed. Inside the plan's File Structure you do not wait on a human: you decide, record the decision as a ruling with what it costs if wrong, and keep going. Anything that leaves this machine, anything that spends, and anything that changes a spec decision beyond the one pre-authorised below are the exceptions.

Your goal is to land SP7c end to end. Done means: the plan has been reviewed against the live tree and every Critical and Warning fixed in the plan file with a numbered deviation entry; every task's RED test was seen failing and its implementation seen passing; every task's files are committed by explicit path with the plan's own commit message; the ai_rfc suite is green under the 3.10 interpreter with its count measured and reported, and the 3.11 optimize selection's count is measured and reported against the baseline you took in Phase 1; every parser, regex or tool invocation the plan wrote against tool output it never captured was probed against the real tool before the row's last task; the row's gate was run with its actual output pasted; the whole-branch review ran, its fix wave landed, and its re-review closed; every deviation is a numbered entry in the row's deviation log, committed in PANTHER; the push and the submodule bump were done on the user's approval at Phase 5, or the exact commands are in the report; and the resume point is in memory. Editing the tasks is not done; the gate, run and pasted, is. The row's gate is the plan's own: Task 7 Step 1's sequence test, restated in Task 8 Step 4, drives a three-cluster sweep with `consolidate_every` 2 through `_stub_spawn` and asserts the round sequence `c1, c2, cons, c3, cons(final)` by the prompt file each session was launched with; the whole ai_rfc suite is green; the five SP7b goldens under `tests/substrate/draft/goldens/` are unchanged; the frozen-prompt tests in `tests/experiment/test_render.py` pin the regenerated loop skill byte for byte; and the SP7b MARK probe, the strict draft gate on a fresh scratch copy of the sealed MARK baseline, still prints `note: gate clean` with exit 0 and an empty findings list, because SP7c changes nothing on the gate side and that probe is the cheapest proof of it.

Three decisions the user took on 2026-09-09 shape this row, and each is a ruling you record in the deviation log's entry 0 before any task runs. First, sequencing: the one-door spec's decision D55 orders SP7c after CLI-2 and CLI-3, and the 2026-09-05 executor prompt says SP7c is not a row it runs because its plan targets the experiment driver CLI-2 replaces; the user has ruled that SP7c runs now, before CLI-2, on the experiment driver as it stands after CLI-1, and that CLI-2 inherits the migration of the consolidation hook into `ai_rfc/driver/`. Your handoff therefore lists, file by file, what CLI-2 must move. Second, the prompt-text fix: the CLI-1 session found that `ai_rfc/experiment/render.py` still hands the model the old lax progress contract in two places, the arm-C hand instruction for `cluster_next` at about lines 83 to 87 ("take the first id that has neither a `checkpoints/<id>/` directory nor a `revisions.yaml` entry") and the MCP tool description at about lines 255 to 257 ("returns the lowest-ordinal cluster with neither checkpoint nor revision entry"); under the strict ledger rule the raw arm and the MCP arm would compute different next clusters, and no campaign may run until it is fixed. The user has ruled that Task 4, which already edits `render.py` and freezes the prompts, corrects both slot texts to the ledger's rule: a cluster is done when its checkpoint exists under `checkpoints/<id>/`, `revisions.yaml` holds a `kind: cluster` entry for it, and that entry's tag exists in the draft repository; the next cluster is the lowest-ordinal in-window cluster that is not done, pre-seeded clusters counting as done; the source of truth is `ai_rfc.ledger.next_cluster` and `ClusterState.done` in `ai_rfc/ledger.py`. The two texts must say the same rule in each arm's own vocabulary, `test_render.py`'s golden is re-pinned, and `write_plugin_skill` regenerates `plugins/ai-rfc/skills/ai-rfc-reconstruction-loop/SKILL.md`; the deviation entry names the ruling and its cost, which is that a prompt change alters what a campaign measures and lands in the row that already changes the prompts. Third, the spec edit: Task 9 Step 3 replaces one cell of the spec's roadmap row for SP7c in `docs/superpowers/specs/2026-09-02-arfc-draft-quality-design.md` (the gate cell reading "`fake_claude` sequence `c1,c2,cons,c3,cons(final)`", at about line 187) with "`_stub_spawn` sequence `c1,c2,cons,c3,cons(final)`, asserted on the prompt each session was launched with", because `fake_claude` replays one scenario for one run id and cannot work a window; the user pre-authorised exactly that edit, and any other spec edit is a stop. A fourth decision bounds scope: SP7b's residue list (below) rides along only where a task already edits the file and the fix is small; nothing else from it enters this row. A fifth: nothing in this row spends. No `claude -p` in any role; the consolidation prompt is verified by rendering and by the frozen-prompt tests, never by running it.

The context you work in. The PANTHER worktree is `/Users/elniak/Documents/Documents/Work/Project/Protocol-Testing-Security/PANTHER/master/.claude/worktrees/iut-ai-rfc`, branch `feat/arfc-pipeline-and-runtime-anchors`, in the Claude Code harness on macOS with the sandbox on by default. Two interpreters: `.venv/bin/python` in the worktree (Python 3.10; the harness, the MCP server and every test that does not import gepa) and `~/ai-rfc-experiments/venv-optimize/bin/python` (Python 3.11, the only interpreter that imports gepa; it lives outside the tree on purpose, because a macOS watcher re-hides every `.pth` file under this worktree within about ninety seconds and 3.11 skips hidden `.pth` files while 3.10 does not; the former in-tree `.superpowers/venv-optimize` is broken and nothing runs from it). The `ai_rfc` project is its own git repository (remote `https://github.com/ElNiak/ai_rfc.git`, branch `main`), consumed by PANTHER as the submodule at `panther/plugins/services/testers/ai_rfc` and installed editable into both interpreters; the submodule has no pre-commit hook of its own, while PANTHER's pre-commit hook stashes and restores every unstaged file around a commit. Landed state on 2026-09-08, all pushed: ai_rfc `main` at `3fde91b` (CLI-1's last commit), PANTHER at `08746f0fe` pointing the submodule at `3fde91b`, both trees clean. Measured then and to be re-measured by you in Phase 1: the ai_rfc suite under 3.10 from the submodule root is 1406 passed and 11 skipped (`SSLKEYLOGFILE= <worktree>/.venv/bin/python -m pytest tests -q -n auto -p no:cacheprovider`, about two minutes); PANTHER's door tests `tests/unit/test_cli/test_ai_rfc_commands.py` are 6 passed from the worktree root; the 3.11 optimize selection is `tests/experiment/optimize tests/experiment/test_cli_optimize.py -q -m "not slow" -p no:cacheprovider`, run only after `~/ai-rfc-experiments/venv-optimize/bin/python -c "import ai_rfc"` succeeds from a cwd outside the package and `~/ai-rfc-experiments/venv-optimize/bin/python -v -c pass 2>&1 | grep -c Skipping` prints `0`; the CLI-1 session recorded that selection as red for reasons outside its files, so a failure there that reproduces at your Phase 1 baseline is pre-existing and reported as such, never fixed in this row. PANTHER's whole unit suite is not run; only one PANTHER file outside `docs/` may change in this row, and none is planned.

The ai_rfc layout after CLI-1. `ai_rfc/` is the substrate (`check`, `history`, `forge`, `timeline`, `views`, `draft` with `build.py`, `lint.py`, `gate.py`, `checkpoint.py`, `structures.py` and `completeness.py`, `coverage`, `pipeline`, `schema.py`, `promotion.py`, `anchors.py`, `cli.py` the dispatcher, `entrypoints.py`, plus, since CLI-1, `config.py` for `recon.yaml` and `ledger.py`, the one progress reader every surface agrees with, with `LedgerError`, `ClusterState`, `clusters`, `next_cluster`, `window_of`, `counts`). `ai_rfc/lifecycle/` holds the one-door verbs as packages `<verb>/{__init__,__main__,cli}.py` for `init`, `run`, `status`, `verify`, `doctor`, `config` and `toolchain`, with shared `workspace.py`, `common.py` and `profile.py`; `experiment/paths.py` was deleted. `ai_rfc/server/` is the MCP server with twenty tools in `tools.py` and the parity CLI `ai_rfc <verb>`; `server/core/` holds `revisions.py` (`record_revision(ctx, tag, cluster_id, normative_change, note, kind="cluster", checkpoint=None)`, raising `CoreError`), `structures.py`, `gates.py`, `queries.py` (whose `cluster_next` delegates to `ai_rfc.ledger`), `build.py` and `claims.py`. `ai_rfc/experiment/` is the driver and instrument this row edits: `per_cluster.py` (`run_per_cluster` at about line 252, its loop calling `window_progress` at about line 296 and returning on `row is None`), `config.py` (`CampaignConfig` and the `Campaign` record, a dataclass ending in defaulted fields, `session_mode` and `task` the precedents; `load_campaign` does `Campaign(**payload)`), `runner.py` (`prepare_run_argv(campaign, ref, task=None, ..., prompt_file=None)`, defaulting the prompt to `campaign.prompts_dir / f"arm-{ref.arm}.md"`), `render.py` (`NEUTRAL_TEXTS`, `SLOT_TABLES` keyed `interactive`, `C`, `B`, `A`, `TASK_PROFILES` with `loop` and `interview`, `render_loop`, `arm_prompt`, `write_plugin_skill`), `progress.py`, `metrics.py` (`cluster_artifacts` and `window_clusters` now read `ai_rfc.ledger`), `summary.py` (`revision_of` still joins revisions by `cluster_id`, first match wins; `_previous_tag` decrements the tag suffix), `cli.py` (`campaign init`, `run`, the `optimize` verb group), `spawn.py`, `arms.py`, `enforcement.py`, `guard.py`, `audit.py`, `prompts/` (`loop.tmpl.md` with steps 1 to 10 as literal text and no step 3b, `task.md`, `task-interview.md`, `draft-skeleton.md`) and `optimize/` (the GEPA track's code, which imports `experiment.config`, `experiment.render` and `experiment.workspace`; it must keep importing). `plugins/ai-rfc/skills/` holds `ai-rfc-rfc-style`, `ai-rfc-figures`, `ai-rfc-structures`, `ai-rfc-evidence-hygiene`, `ai-rfc-interviewing` and the generated `ai-rfc-reconstruction-loop`; there is no `ai-rfc-editorial`, which this row adds. Tests: `tests/substrate`, `tests/server`, `tests/experiment` (with `tests/experiment/optimize/`, the fake agents `tests/experiment/fake_claude/claude` and `claude-lm`, and `_stub_spawn` at `tests/experiment/test_per_cluster.py:98`, which monkeypatches `per_cluster.spawn` and patches `cluster_artifacts` in both `per_cluster` and `progress`); `pyproject.toml` runs pytest with `--import-mode=importlib`. The three doors `ai-rfc <verb>`, `python -m ai_rfc <verb>` and `panther ai-rfc <verb>` are one dispatcher. Anything describing a `harness/` directory, a PANTHER-side substrate, `PANTHER_REPO` or `python -m panther.plugins.services.testers.ai_rfc` is obsolete.

The plan is `docs/superpowers/plans/2026-09-03-arfc-draft-quality-sp7c.md` in PANTHER, 1,407 lines, Task 0 plus nine tasks, last revised by `26ba0b97c` on 2026-09-03 (that commit's message notes a `consolidate_every` collision; read it). It has no deviation log yet; you create `docs/superpowers/plans/2026-09-03-arfc-draft-quality-sp7c-deviations.md` beside it, numbered from D1, with entry 0 holding the rulings above. Its Global Constraints, Interface contract (C1 to C16), File Structure, Execution section and every task's Interfaces block stay in front of you. What is already known about the contract on the 2026-09-09 tree, to be re-verified by you in Task 0 rather than trusted: C1 to C5 landed in SP7b (`write_consolidation_checkpoint(manifest_path, ordinal, base_checkpoint, cluster_id, out)` refusing an ordinal outside 1..99; `RevisionEntry.kind` and `run_gate(..., consolidations_dir=None)`; `record_revision` with `kind=` and `checkpoint=`; twenty tools; the skill list above); C7 holds (`prompt_file=` exists, Task 4 uses it); C9 holds (steps 1 to 10 literal, no 3b); C10 holds with `TASK_PROFILES` added beside `SLOT_TABLES`; C11 holds; C12 moved: `cluster_artifacts` no longer joins revisions itself but delegates to `ledger.clusters`, and `ledger._entries` already skips every entry whose `kind` is not `cluster` (a CLI-1 change), so Task 3's C12 filter is satisfied at the ledger and its RED test for that half must be rewritten to prove the ledger-level filter rather than to add a second one; C13 still holds as written (`summary.revision_of` is unfiltered and Task 3 filters it); C14 is deliberately not filtered, as the plan says. The plan's Execution order is binding: Tasks 1 to 3 in any order first; Task 6 before Task 4 (the skill file must exist for `consolidation_prompt` to render); Task 5 after 4; Task 7 after 2 and 4; Task 8 after 7; Task 9 last. Task 9 Step 4's own pointer bump and pushes are superseded by the Phase 5 handover below. SP7b's residue, for the ride-along rule: `upsert_claim` still passes `SchemaError` where its siblings raise `CoreError`; an empty-string `base` passes both checkpoint guards; the `defined − current` lint guard; `claims.py`'s private helpers imported by three modules; `JSONDecodeError`, `YAMLError` and `KeyError` escaping `record_revision` on a corrupt file; `_states` checks elements, not the container; `audit.edit_target` classifies `consolidations/` as `other`; `--timeline` is required but unread on the consolidation checkpoint path; the tool parameter named `fields` carries the whole body; R27's checkpoint-path guard is lexical and ignores symlinks. Of these, `audit.edit_target` and the `--timeline` flag are the two most likely to sit in a file SP7c touches.

The specs, every decision of which is settled: `docs/superpowers/specs/2026-09-02-arfc-draft-quality-design.md` (D38 to D52; D43 the consolidation round every K and at sweep end with no in-session subagents, D48 its checkpoint under `consolidations/<NN>/`, D41 figures whose captions cite, D50 the scheduler consumes SP4's ledger if it lands first, which it has, D52 a consolidation with `normative_change: false` keeps every citation, a mid-sweep failure is recorded and the sweep continues, a sweep-end failure exits 1), `docs/superpowers/specs/2026-09-03-arfc-one-door-design.md` (D53 to D61 and its "Sequencing (D55)" section, overridden for this row only by the user's sequencing ruling above) and `docs/superpowers/specs/2026-09-02-ai-rfc-extraction-and-flow-design.md`. The preceding rows' records: `docs/superpowers/plans/2026-09-03-arfc-draft-quality-sp7a-deviations.md` (D1 to D38), `…-sp7b-deviations.md` (D1 to D33) and `…-one-door-cli1-deviations.md` (D1 to D22), plus the SP7b ledger at `.superpowers/sdd/2026-09-03-arfc-draft-quality-sp7b/progress.md` (rulings R1 to R48) and the CLI-1 ledger at `.superpowers/sdd/2026-09-03-arfc-one-door-cli1/progress.md`, both gitignored and kept. Project memory is `/Users/elniak/.claude/projects/-Users-elniak-Documents-Documents-Work-Project-Protocol-Testing-Security-PANTHER-master/memory/` with the index `MEMORY.md`; before anything else read `handoff-2026-09-05-arfc-sp7b.md`, `handoff-2026-09-08-arfc-cli1.md`, `handoff-2026-09-03-arfc-draft-quality-v2.md`, `feedback_ask_what_else_a_normalised_character_can_do.md` and the sub-index `index-feedback-process-harness.md`.

Evidence roots, read-only: `~/ai-rfc-experiments/` (the sealed baseline `baselines/mark-a1-2026-09-03` with its `.sha256`, the campaigns `campaigns/mark-full` and `mark-full-1`, the pristines, `tools/toolchain.json`, the authenticated profile `profile/` which nothing in this row touches), `~/arfc-experiments/` (the older root), and `reconstructions/mark` in the worktree (gitignored). Never run `python -m ai_rfc.experiment audit|analyze` against any campaign under either root; never write into a baseline, campaign, pristine, tools, profile or reconstruction directory. Scratch for probes is `/tmp/claude/sp7c/`, which is cleared between sessions, so recreate it: the MARK probe copies `~/ai-rfc-experiments/baselines/mark-a1-2026-09-03/workspace` to `/tmp/claude/sp7c/mark`, makes the copy writable, removes the copied `out/gate-report.json`, and runs `SSLKEYLOGFILE= <worktree>/.venv/bin/python -m ai_rfc.draft gate /tmp/claude/sp7c/mark/draft --timeline /tmp/claude/sp7c/mark/timeline --checkpoints /tmp/claude/sp7c/mark/checkpoints --questions /tmp/claude/sp7c/mark/questions.yaml --revisions /tmp/claude/sp7c/mark/revisions.yaml --out /tmp/claude/sp7c/mark/out --strict` from the submodule root with the sandbox off and a Bash timeout of at least 300 seconds (about 30 seconds of work; the tool's 100-second default has killed it once). Never run `panther docs build` or `panther_builder.py clean|package-dev`; both delete `docs/`. Files under PANTHER's `docs/` are gitignored but tracked, so they need `git add -f`.

The constraints are not negotiable. No Anthropic API key is used anywhere in this project: no step sets, reads or prints `ANTHROPIC_API_KEY`. Never pass `--no-verify`. Never run `git add -A` or `git add .`; stage by explicit path, and run `git status --short` in every repository you touch before every commit. If status lists files you did not touch, do not stage them; if any of them is staged by someone else, commit with `git commit --only -m "<message>" -- <your paths>` and confirm with `git show --stat HEAD` that only your files are in the commit. Never commit in PANTHER while a file you do not own shows as modified there; hold the commit and re-check before each later attempt. A pathspec commit takes a file's whole working-tree content, so never edit a file another session is editing at the same time. Never run `git stash` in a shared worktree, never amend a commit that is already on the branch, never move a submodule pointer backwards, and never `git checkout --` a file you did not write. Commit messages follow the plan's own (`type: lowercase summary` in ai_rfc, `type(scope): lowercase summary` in PANTHER, no trailing period); every commit ends with a blank line and the trailer `Claude-Session: <this session's URL>`, passed as a second `-m`, and confirmed on the first commit with `git show -s --format=%B HEAD`. Run `pytest`, `pip`, nested git and submodule operations and `git push` with the sandbox off, and prefix every `pytest` and `python -m` invocation with `SSLKEYLOGFILE=`. Lint with `black`, `isort --profile black`, `flake8 --max-line-length=88` and `mypy --follow-imports=silent`, on the task's own files only, because the repository carries lint debt (`ai_rfc/cli.py` alone has nine pre-existing mypy errors). The worktree guard refuses git inside loops, variables, compound constructs and heredocs, and refuses any command whose target is computed at runtime: run plain git commands with literal paths, and when you need to append a long text to a ledger or a log, write it to the scratchpad with the Write tool and `cat` it into place. No agent may write `CLAUDE.md`; record any edit it needs verbatim in your report. Do not touch files outside a task's listed files unless a ruling extends the list within the plan's File Structure or the ride-along rule applies, do not add features the plan does not name, do not reopen a decision a spec records, and do not contact a peer session except to send one factual note when you are about to touch something it owns; never wait for a reply. Nothing in this row runs a model: no `claude -p`, no campaign init, no experiment run, no optimisation run.

Work in five phases. Phase 0 is the gate. If the NEXT line still holds a placeholder, stop and ask. Run `git branch --show-current`, `git log -1 --format='%h %ad %s' --date=format:'%H:%M:%S'` and `git status --short` in both repositories, and `git ls-tree HEAD panther/plugins/services/testers/ai_rfc` from the worktree root. Confirm SP7b and CLI-1 landed by their artifacts, not by memory: the SP7b deviation log records D33, the CLI-1 deviation log exists, `ai_rfc/ledger.py` and `ai_rfc/lifecycle/` exist, `ai_rfc/schema.py` loads a `structures:` block, `ai_rfc/server/tools.py` lists twenty tools, and the recorded gitlink is at or past `3fde91b`. Then run `ListAgents`: a session of the one-door CLI track (the last one was named `ai-rfc-one-door-cli`) that is busy, or any live session with an uncommitted edit under `ai_rfc/experiment/`, is a stop, because SP7c and CLI-2 write the same files and the user ruled that SP7c runs alone; an idle CLI-track session is not a stop. A dirty tree you did not cause and cannot attribute to a live peer is a stop, reported with the mismatch and nothing edited. A dirty tree a predecessor session of this same row left behind is not a stop: read the row's ledger at `.superpowers/sdd/2026-09-03-arfc-draft-quality-sp7c/progress.md`, the deviation log and `git log`, treat the predecessor's uncommitted edits as this row's own work to verify, never delete them, and resume from the first task without a `complete` line in the ledger; ask only if the ledger and git disagree. Open the ledger with the landed state and the `ListAgents` output; it is rewritten at every task boundary and every stop.

Phase 1 is exploration. Read the plan in full, in chunks, and keep its header, Global Constraints, Interface contract, File Structure, Execution section and every task's Interfaces block in front of you. Read the three preceding deviation logs and the five memory files named above. Measure the baseline yourself: the 3.10 suite from the submodule root, the door tests from the worktree root, and the 3.11 optimize selection after its two checks; the measured numbers win over the plan's and over this prompt's. Run the plan's Task 0 as written, every grep with a literal path, and record each contract item's verdict in the ledger; where a verdict differs from what this prompt says, the tree wins and the difference is a deviation entry. When you have several independent files to read or unrelated commands to run, batch them in one response.

Phase 2 is planning, and this row has none: the plan exists and is adopted as it stands. Do not invoke the brainstorming or plan-writing skills.

Phase 3 is review. No deviation log exists and the plan was written three rows ahead of the tree it now meets, so invoke `review-plan` with `Skill(skill="review-plan", args="docs/superpowers/plans/2026-09-03-arfc-draft-quality-sp7c.md")` once, on the whole plan, and read its annotated output for what a moving branch breaks: the precondition; the plan's test imports, twenty-two of which name the pre-SP1 package `experiment.` where the landed package is `ai_rfc.experiment` (a Critical the SP7b review found in the same place); every `file:line` anchor re-checked by symbol; exact-match edit blocks over `experiment/per_cluster.py`, `config.py`, `runner.py`, `render.py`, `metrics.py`, `summary.py`, `cli.py` and `prompts/loop.tmpl.md`, all of which SP7a, SP7b or CLI-1 touched; Task 3 against the ledger-level filter described above; Task 4's `render.py` edits against `TASK_PROFILES` and against the two slot texts the user's ruling adds; Task 8 against CLI-1's argparse root and the `campaign init` parser as it now stands; contract items against the landed code; test needles that could match a fixture's own name; tests that assert nothing; the sibling of every named file; every construction site of a class the plan makes stricter (`Campaign` gains a field; `test_a_campaign_frozen_before_the_field_existed_still_loads` is the precedent it must satisfy); and task ordering. Fix every Critical and Warning in the plan file itself, record each as a numbered deviation entry, commit the revised plan and its log in PANTHER with `git add -f` (`docs(ai_rfc): …`), and only then execute; a plan with an unresolved Critical is not executed, and a Critical that would change a spec decision other than the pre-authorised gate cell is a stop. Do not invoke `review-plan` a second time for the same tree.

Phase 4 is execution. Invoke `superpowers:subagent-driven-development` with the plan path and follow it: a fresh implementer per task, a reviewer between tasks who reads the diff package and the implementer's report rather than the implementer's summary alone, every seat on the most capable model available (`opus`), one implementer at a time and never two in parallel. Do not use `superpowers:executing-plans` or inline batching; the plan is sized for the subagent mode. The mechanics the skill leaves to you: keep the row's SDD workspace at `.superpowers/sdd/2026-09-03-arfc-draft-quality-sp7c/` in PANTHER's root; cut each task's brief with the skill's `task-brief` script; write `global-constraints.md` (the plan's Global Constraints verbatim, plus the five rulings above) and `implementer-rules.md` (literal paths, sandbox, git hygiene, the trailer, the report contract) once and reference them from every dispatch; because the commits live in the nested repository, run the skill's `review-package` script from the submodule's directory with the plan path and an explicit output file `review-nested-<base>..<head>.diff` in the workspace, cut over the task's own files only, and tell the reviewer which SHAs in the range belong to peers; record BASE before every dispatch and run `git merge-base --is-ancestor <BASE> HEAD` before every commit; give every reviewer and implementer a report file to write to, with the reply limited to a verdict block of under thirty lines, because long replies truncate in transit. Every review brief carries the author-controlled-text question from the role paragraph; for this row the artifacts it applies to are the rendered consolidation prompt (a skill body and the `CONSOLIDATION_TEXTS` are interpolated into it), the `revisions.yaml` entry a consolidation writes, and the `Due` record the scheduler derives from disk. Findings a reviewer labels plan-mandated are yours to rule on against the spec, not to dismiss. Minor findings go to the ledger as deferred, except a one-line correction inside a file the fix round is already touching. A fix round is one dispatch plus one scoped re-review; rounds one to three resume the same implementer, rounds four and five dispatch a fresh one carrying the brief and the report file. Before Task 9, run the real-tool probe step: for every parser, regex or tool invocation the plan wrote against tool output it never captured (at least `consolidation_due`'s reading of `revisions.yaml` and `consolidations/<NN>/` on a real SP7b-shaped workspace, and the rendered `consolidation-<arm>.md` for each arm), run one real invocation on scratch under `/tmp/claude/sp7c/`, paste the raw output into the ledger, and rule on every mismatch. When all tasks are complete, dispatch the whole-branch review with the skill's code-reviewer template on `opus`, pointing it at the ledger's deferred-minor lines, the rulings list, the deviation log and a context file that names which commits in the range are yours and which belong to peers; then one fix wave for its findings, one scoped re-review, and rulings for any residue. When a subagent stalls, act on evidence rather than waiting: a named subagent that reports a usage limit gets a fresh implementer on the same brief and report file directly, with the sentence "a prior implementer attempted this; read the report file for what was tried"; one whose report file's modification time has not changed for about fifteen minutes while it shows as running gets one probe first, a `haiku` agent asked to reply with a single word. Named subagents deliver only at turn boundaries, so end your turn to receive them rather than polling; every finished subagent also sends a duplicate idle notice, which needs no action. Before marking any task, the plan or the row complete, invoke `superpowers:verification-before-completion`: run the commands, read the output, report it.

Phase 5 is the gate, the handover and the report. Run the row's gate as defined in the goal paragraph, with the sandbox off where the constraints say so, and paste every output: the 3.10 suite with its count, the 3.11 selection with its count against the baseline, the door tests, the sequence test, the goldens' status, the frozen-prompt tests, and the MARK probe's note, exit code and findings. A gate that fails gets one fix wave and one scoped re-review; if it still fails, the row stops here: write the resume point, report the failing output, and end the turn. A gate that passes leads to the handover, asked once with `AskUserQuestion`: fetch both remotes first (`git fetch origin` in the submodule and in the worktree, with the sandbox off) and confirm `origin/main` and the PANTHER branch are ancestors of your heads; put in front of the user the push command for ai_rfc (`git push origin <gate-tested sha>:main` when peer commits sit above the tested SHA, naming them; a plain `git push origin main` otherwise), the recorded gitlink from `git ls-tree HEAD panther/plugins/services/testers/ai_rfc`, the bump command (`git add panther/plugins/services/testers/ai_rfc` when the checkout's HEAD equals the tested SHA; `git update-index --cacheinfo 160000,<tested sha>,panther/plugins/services/testers/ai_rfc` when peers have committed past it), and the PANTHER push command; offer no bump if a peer already bumped past your head. On a yes, push ai_rfc, verify with `git log --oneline -1 origin/main`, confirm `git merge-base --is-ancestor <recorded> <tested sha>`, commit the bump as `chore(ai_rfc): bump ai_rfc to SP7c (<old>..<new>)`, push PANTHER, and verify both remotes; if either remote moved between the fetch and the push, stop and report rather than push. Delete `/tmp/claude/sp7c/` only after the bump.

On risk, you decide without asking everything inside the plan's File Structure: reads, edits, test runs, lint, plan-file corrections found in review, rulings on plan-text defects, cross-task test plumbing a stricter constructor forces, order changes a fixture dependency forces, an edit outside the File Structure when a ruling can justify it against the plan's intent or the ride-along rule (record it, with its cost if wrong), and commits once the status check is clean. You ask first, and stop until answered, before four kinds of action: anything that leaves this machine or moves a shared pointer (the Phase 5 handover, asked once); any `claude -p` launch in any role, which this row does not have and which is therefore always a stop; a deviation that would change a decision the draft-quality spec or the one-door spec records, beyond the pre-authorised gate cell; and deleting an SDD workspace or any directory under an evidence root. Destructive classes to name to yourself before running them, and never run as a shortcut: `rm -rf` outside `/tmp/claude/`, `git reset --hard`, `git push --force`, `git clean`, moving directories under an evidence root, and anything that posts outside this machine.

Report as you go and at the end, in neutral technical prose, no emoji. During execution, one line per task boundary is enough: the task number and name, RED seen failing, GREEN seen passing, the commit hash, the suite count. Keep the deviation log beside the plan, one entry per deviation with three fields (what the plan said, what the code or the tool showed, what you did), numbered from D1 after entry 0, and committed in PANTHER together with the task that produced it. Your final message stands alone for someone who did not watch and stays under about 900 words: the row and the plan it ran; what landed, as commits in every repository and the submodule pointer state; the suite counts before and after under both interpreters and the door tests; the per-task one-liners; the deviations by number with one line each; the gate's output, pasted; a "Rulings I made" list, every ruling in the order you made it with what it costs if wrong; a "For the user" list, verbatim, of everything deferred because it needed them (commands not yet run, uncommitted PANTHER files with their command, `CLAUDE.md` edits, the residue left for a later row); a peer-session ledger naming which commits in the range are another session's and which files it held at the end; the CLI-2 migration list, file by file, of what SP7c put into `ai_rfc/experiment/` that CLI-2 must move into `ai_rfc/driver/`; anything left undone and why; and the next row (CLI-2, whose brainstorm now starts from a landed consolidation hook rather than a planned one) with the state of its prerequisites. Then write the resume point to memory: a file `handoff-<date>-arfc-sp7c.md` in the memory directory with the frontmatter this project's memories use (`name`, `description`, `metadata.type: project`), holding the facts that are not derivable from git or from the ledger, and one index line in `MEMORY.md` under "Active Work", replacing the SP7b line; `MEMORY.md` is already over its size target, so trim only lines you own or move a section to a sub-index file. Report failures with their output; never describe unverified work as done.

When the plan and the code disagree, fix forward and log it: a moved anchor is re-anchored by symbol; a function or contract the plan names but the code lacks is traced first (`git log -S<name>` in the owning repository and the preceding rows' deviation logs) and adapted when the intent is unambiguous; a step whose edit block no longer matches is rewritten to the same effect; a plan snippet the review or a probe proves wrong is corrected by ruling, with its cost if wrong. On decisions, the draft-quality spec wins over the plan, the user's five rulings above win over both specs where they conflict, and on facts the code and the tool win over all of them. Stop and ask instead when the Phase 0 gate or the plan's precondition fails (report the mismatch, edit nothing), when a RED test passes against the unfixed code (the plan's test is wrong, and a wrong test hides a wrong fix), when the suite regresses outside the task's scope, when a deviation would change a spec decision other than the pre-authorised one, when the gate fails after its one fix wave, or when a design point the plan did not settle turns out to be load-bearing mid-row (raise it as one question with your recommendation, not as a ruling). A suite count that differs from the plan's or this prompt's expectation is measured and recorded, never reconciled by skipping or weakening tests. If HEAD moves under you mid-task, re-read the files you are about to edit before editing; if pre-commit modifies files during a commit, re-stage them and commit again; if a peer's unstaged files appear, wait and re-check rather than stashing, and if a peer's staged files appear, use the `--only` commit form; if a peer reports that it pushed or bumped a pointer on the user's ruling, verify it in git and adjust rather than repeating or reversing it.

Skills available to you, by exact name: `review-plan`, invoked once in Phase 3 on the plan path (not again for the same tree, and not on any other file); `superpowers:subagent-driven-development`, invoked with the plan path at the start of Phase 4 (not before Phase 3 has closed every Critical, and never substituted by `superpowers:executing-plans`); `superpowers:verification-before-completion`, invoked before you mark any task, the plan or the row complete. Begin with Phase 0.
