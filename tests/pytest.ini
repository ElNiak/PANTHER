# File: pytest.ini
[pytest]
addopts = --strict-markers --disable-warnings --cov-branch --cov-append --cov=panther --cov-report=html -vv --debug=pytest_cov --cache-clear --maxfail=0 --continue-on-collection-errors
markers =
    unit: marks tests as unit tests (deselect with '-m "not unit"')
    integration: marks tests as integration tests (deselect with '-m "not integration"')
    property: marks tests as property-based tests
    e2e: marks tests as end-to-end tests
log_cli = true
log_cli_level = DEBUG
log_file = pytest.log
log_file_level = DEBUG
